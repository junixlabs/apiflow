# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.playwright-mcp/
.claude/specs/

# Local agent config — chứa nội bộ (sidcorp / epodsystem), không được publish
.claude/
.mcp.json

# cm:why apiflow's own scan output, when the tool is pointed at this repo while dogfooding.
# Committing it would version a snapshot of this repo's own map that nothing reads.
.apiview/
