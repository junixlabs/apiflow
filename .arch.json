{
  "version": 1,
  "modules": {
    "core": [
      "src/core/**"
    ],
    "engine": [
      "src/engine/**"
    ],
    "utils": [
      "src/utils/**"
    ],
    "types": [
      "src/types/**"
    ],
    "store": [
      "src/store/**"
    ],
    "hooks": [
      "src/hooks/**"
    ],
    "components": [
      "src/components/**"
    ],
    "mcp": [
      "src/mcp/**"
    ],
    "proxy": [
      "proxy/**"
    ]
  },
  "tests": [
    "**/*.test.ts",
    "**/*.test.tsx",
    "**/*.spec.ts"
  ],
  "exclude": [
    "**/node_modules/**",
    "**/dist/**",
    "**/build/**",
    "**/coverage/**",
    "**/.forge/**",
    "**/.claude/**",
    "**/*.d.ts",
    "public/**",
    "skills/**",
    "docs/**"
  ],
  "contracts": [
    {
      "id": "core-is-headless",
      "type": "forbidden",
      "from": "core",
      "to": "*",
      "status": "locked",
      "description": "src/core is the headless layer shared by the React app and the MCP server; CoreApiNode exists precisely to break the xyflow coupling. Measured 19/08/2026: 0 outgoing cross-module edges."
    },
    {
      "id": "proxy-is-standalone",
      "type": "forbidden",
      "from": "proxy",
      "to": "*",
      "status": "locked",
      "description": "the proxy is a separate process and must not reach into app modules. Measured 19/08/2026: 0 outgoing cross-module edges."
    },
    {
      "id": "mcp-not-ui",
      "type": "forbidden",
      "from": "mcp",
      "to": "components",
      "status": "locked",
      "description": "the MCP server runs headless; importing a React component would drag xyflow into a stdio process. Measured 19/08/2026: 0 edges."
    },
    {
      "id": "engine-is-a-shim",
      "type": "forbidden",
      "from": "engine",
      "to": "store",
      "status": "draft",
      "description": "src/engine is documented as a re-export shim over src/core, but 2 edges reach into store today. Draft until those are removed."
    }
  ]
}